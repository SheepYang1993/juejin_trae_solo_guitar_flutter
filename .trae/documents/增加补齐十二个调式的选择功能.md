1. **扩展调列表**：

   * 在 `lib/main.dart` 中修改 `_keys` 列表，从当前的 `['C', 'G', 'D']` 扩展为包含所有十二个调式

   * 使用五度圈顺序：`['C', 'G', 'D', 'A', 'E', 'B', 'F#', 'Db', 'Ab', 'Eb', 'Bb', 'F']`

2. **验证音阶生成逻辑**：

   * 确保现有的 `_updateScale` 方法能够处理所有十二个调式

   * 验证 `Note.fromName` 方法能够识别所有调式的音名

3. **运行测试**：

   * 运行应用程序，测试所有十二个调式的选择功能

   * 验证生成的音阶是否正确

   * 确保界面显示正常

4. **优化用户体验**：

   * 确保调选择下拉菜单能够正常显示所有十二个调式

   * 测试调式切换功能是否流畅

5. **更新文档**：

   * 记录调式扩展的变更

   * 确保文档与代码保持一致

