1. **扩展音阶类型枚举**：

   * 在 `lib/models/scales.dart` 中修改 `ScaleType` 枚举，添加以下新的音阶类型：

     * `naturalMinor` (自然小调)

     * `harmonicMinor` (和声小调)

     * `melodicMinor` (旋律小调)

     * `blues` (蓝调音阶)

     * `dorian` (多利亚调式)

     * `phrygian` (弗里几亚调式)

     * `lydian` (利底亚调式)

     * `mixolydian` (混合利底亚调式)

     * `locrian` (洛克里亚调式)

2. **实现新的音阶工厂方法**：

   * 在 `Scale` 类中添加以下工厂方法：

     * `Scale.naturalMinor()` - 自然小调

     * `Scale.harmonicMinor()` - 和声小调

     * `Scale.melodicMinor()` - 旋律小调

     * `Scale.blues()` - 蓝调音阶

     * `Scale.dorian()` - 多利亚调式

     * `Scale.phrygian()` - 弗里几亚调式

     * `Scale.lydian()` - 利底亚调式

     * `Scale.mixolydian()` - 混合利底亚调式

     * `Scale.locrian()` - 洛克里亚调式

3. **更新主应用界面**：

   * 在 `lib/main.dart` 中修改 `_scaleTypes` 列表，添加新的音阶类型选项

   * 确保新的音阶类型能够正确显示在下拉菜单中

4. **更新音阶切换逻辑**：

   * 在 `_updateScale` 方法中添加对新音阶类型的处理

   * 确保所有新音阶都能正确生成

5. **运行测试**：

   * 运行应用程序，测试所有新音阶的选择功能

   * 验证生成的音阶是否正确

   * 确保界面显示正常

6. **优化用户体验**：

   * 确保音阶类型选择下拉菜单能够正常显示所有音阶类型

   * 测试音阶切换功能是否流畅

7. **更新文档**：

   * 记录音阶扩展的变更

   * 确保文档与代码保持一致

