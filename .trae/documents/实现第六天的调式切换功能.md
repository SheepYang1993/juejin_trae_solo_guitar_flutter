1. **扩展音阶列表**：

   * 在 `lib/main.dart` 中扩展 `_scales` 列表，添加C调、G调和D调的各种音阶类型

   * 包括大调、大调五声音阶和小调五声音阶

2. **改进调式选择界面**：

   * 将当前的单一下拉菜单拆分为两个下拉菜单：一个用于选择调，一个用于选择音阶类型

   * 实现调与音阶类型的联动，确保生成正确的音阶

3. **实现调式切换逻辑**：

   * 根据用户选择的调和音阶类型，动态生成相应的音阶

   * 确保音阶生成逻辑正确，支持C调、G调和D调

4. **优化用户界面**：

   * 添加更清晰的标题和说明文字

   * 优化布局，提高用户体验

   * 确保界面响应式，适应不同屏幕尺寸

5. **测试调式切换功能**：

   * 运行应用程序，测试不同调式的切换

   * 验证生成的音阶是否正确

   * 确保界面显示正常

6. **更新文档状态**：

   * 将 `doc/第6天.md` 中的所有工作事项状态更新为"已完成"

