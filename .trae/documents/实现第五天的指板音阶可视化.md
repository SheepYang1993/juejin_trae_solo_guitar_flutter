1. **扩展吉他指板模型**：
   - 在 `lib/models/guitar_fretboard.dart` 中添加计算音符位置的方法
   - 实现根据弦号和品号计算音符的功能

2. **扩展指板绘制组件**：
   - 在 `lib/components/guitar_fretboard_painter.dart` 中添加绘制音阶音符的方法
   - 实现不同颜色区分不同音符的功能
   - 优化音符标记的视觉效果

3. **增强指板组件功能**：
   - 扩展 `GuitarFretboardWidget` 类，添加音阶选择参数
   - 支持显示指定音阶的音符

4. **测试不同音阶显示**：
   - 创建测试用例，验证不同音阶在指板上的显示效果
   - 确保大调音阶和五声音阶都能正确显示

5. **更新文档状态**：
   - 将 `doc/第5天.md` 中的所有工作事项状态更新为"已完成"